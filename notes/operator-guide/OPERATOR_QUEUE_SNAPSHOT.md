# Operator Queue Snapshot

> **PERISHABLE — snapshot 2026-07-30.** This page is the paper mirror of
> the Decision Deck and the open designer-owed items in the records. It
> goes stale by design; **any session that changes the queue refreshes
> this file + date and rebuilds the PDF** (`python3 build_pdfs.py`).
> The deck itself is always the live truth.

Items are grouped by **where you can actually do them** — so a slow
shift, a free evening at the machine, or a rested day-start each have
their own list.

---

## A. From anywhere (phone / dead time — no game needed)

- **Word the loop bar card.** The Gate-1 rewrite reserved the bar's
  final wording for you — the deck card is staged, waiting for your own
  words for "an unguided town-to-boss run that stays fun for me daily
  for a week." Everything downstream quotes this once you write it.
  This is the single best dead-time item on the board.
- **Drop the itch + Discord links into the record.** Both were marked
  done at the burn-down; the links themselves are still owed to the log.
- **Hours backfill numbers.** The backfill was approved; the actual
  numbers are still pending. Reconstruct from memory, hand them to any
  session.
- **Onboarding copy, your voice.** All onboarding-screen text is
  placeholder awaiting your wording. Constraints (documented in the
  file): no coaching, never ask for a play duration. Draft it in notes
  on your phone; any session wires it in.
- *(Maybe phone, if the committed captures read well on a small
  screen:)* **eight-holds round-12 docks verdict** (WorldForge-side)
  and the **tileforge REF3 v2 promotion** call (it gates the
  sprite-forge start).

## B. At the machine — any energy (tired counts; these are not feel)

- **Designer-eyes render checks:** character creation screen, drops +
  HUD (loot band, tier tints, toasts), comments box, feedback toast +
  bundle row, onboarding layout. Just "does it render right" — one
  launch covers all of them.
- **Eyes-closed audio pass** (Law 7), now against the real Resonance
  Forge cues: one mixed encounter, eyes closed — can you tell the cue
  classes apart? Verdict + date go in `game notes/AUDIO_CUE_MAP.md`.
  Cue-mapping and duck taste notes welcome in the same sitting.
- **b71 overworld look-around:** styled paths, the harbor capital
  spawn, the cliff line. (The tops+ramps arc is a separate pending
  design conversation — looking is enough for now.)
- **Weekly GIF** — the cadence item. Loop/king material is fresh;
  `G` → `tools/gif.ps1` → devlog + one community.

## C. Rested day-start only (feel — protect these)

- **THE rested feel pass** — the big one, one session: formally closes
  M2 (movement 3.0/4.0, ranger facing, pause, interp) and ratifies the
  whole provisional bundle — six ordinaries, Rusher slash, arenas,
  sphere set, audio-in-play.
- **b65 city walk verdict** — the acceptance of the cities arc; still
  unrecorded.
- **THE LOOP, first real run:** play THE RUN, call the skeleton
  **judgeable** (that starts the bar clock), then judge every [T] rate —
  drop tables, XP growth, 25% death gold cost, king 900 HP, unique 35%,
  tier tints.
- **Gate-1 deck cards** staged for rested ratification (the CORE-53/55
  rewrite you already ruled Tier 1).

## D. Waiting on others / later (nothing for you to do yet)

- Polished boss sheets from the assembler (drop expected; same
  family:variant slots — revalidate with the tool, then swap).
- world_filler consumption (post-Gate-1 by doc 17).
- The tops+ramps overworld arc (design conversation when you want it —
  talk-before-build applies).
