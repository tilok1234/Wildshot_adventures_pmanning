# Session record — 2026-07-29 (early AM; continuous with 2026-07-28 late)

PC session, designer present and interactive (shift ended 23:00; per the
two-tier ruling below, this is their mid-afternoon). Hours clock running
since 23:20.

## The Decision Deck era (adoption → burn-down, one evening)

1. **Adopted (designer-built tool):** the designer produced a Decision
   Deck app (Claude-design export, single-file offline React; verified:
   zero external requests, decisions persist with option+note+timestamp,
   sample-clear survives reload). Committed as planning
   `tools/decision_deck.html` + README; `test_deck.html` retired;
   Desktop launcher .bat installed. Register convention: EXPORT after a
   deck session → committed as `tools/decision_deck_register.json` →
   session sweeps + executes. 25-card real-queue payload compiled, then
   REWRITTEN IN PLAIN LANGUAGE on designer feedback ("we got to set it
   up so that the questions are easier for me to grasp") — standing
   rule, saved to assistant memory: designer-facing decision text gets
   zero repo jargon. Payload now 27 cards (two world_filler cards
   folded in).

2. **world_filler re-read + doc 17:** the assessment's picture was
   stale — the cut-off F7 freeze review was RESOLVED in a follow-up web
   session (38/38 findings confirmed empirically and fixed; content
   pack format 1 FINAL; TS + headless-Godot verifiers agree
   refusal-for-refusal on a 20-case battery; F8 viewer shipped; 140
   tests). Verified the branch exists on the remote.
   `docs/17-WORLD_FILLER_INTEGRATION_PLAN.md` drafted and (below)
   ACCEPTED: enemyId = game vocabulary, danger bands map to CORE-44
   composition (never stats), spawn draws ride named serialized sim
   streams, ScenarioDef sibling field, vendor the reference addon;
   ALL game-side work post-Gate-1 behind a 5-step ladder. Weekly GIF
   #2 produced from the designer's Warden-fight recording (2.54 MB
   devlog cut; 12 fps / 128 colors to fit a 3 MB cap).

## The burn-down (register: tools/decision_deck_register.json, ~00:47)

20 decisions. Basis: the verdict-system ruling (first card) makes
in-session calls count (TIER 1); feel items keep a rested ratification
(TIER 2). Game CLAUDE.md fresh-hands digest amended accordingly,
including the shift-work definition of "rested" (hours into the
designer's waking day, 15:00–23:00 shift, never wall clock; marathons
and dirty runs stay provisional regardless).

**Rulings:**
- Verdict system → **TWO-TIER** (as above).
- DodgeBot policy of record → **REACTIVE** (ledger #11 CLOSED;
  first_contact adjudication dissolved; the three wolf-pair FAILs
  become documented primary-model baselines; canonical battery
  re-baseline queued).
- Tester recruitment → **10–16 candidates, ≥4 strangers/cycle, ≥5 held
  for cycle 2** (the drafted proposal — the Gate 1 calendar is
  unblocked; the one original-plan ruling never made is now made).
- Warden HP → **RAISE to ~575 [T]** keeping the ~13 s pure-Longbolt
  intent (against the engineering lean — designer's call; elite proofs
  + the full-fight damage schedule re-derive and re-run, queued).
- Fanmaw hue → **KEEP ORANGE** (deutan-bucket risk accepted with the
  capture as evidence; teal remains the recorded alternate).
- Warden actor sheet → **scarecrow:strawfield APPROVED** (still a
  one-line swap when polished art drops).
- Grass slits → **keep sealed now**, with designer note verbatim: "im
  not sure what keep sealed means, but we got to fix this" — recorded
  as PROVISIONAL-KEEP-SEALED pending a plain-language clarification in
  chat; the "fix this" reading = TileForge blocked-ground dressing as
  the endstate (upstream art ask). Do not treat as a clean final until
  clarified.
- Hours backfill → **backfill honestly once, then log forward**
  (designer to supply/confirm numbers; assistant may draft from git +
  session evidence).
- Ledger #12 → **grandfather note recorded** (CLOSED game-side).
- world_filler mainline → **claude/freeze-review-resolution-tf6bkf is
  the lane.** Proper clone landed on this machine (replacing reliance
  on the no-git export; clone HEAD adds F9 studio phases A+B — the
  tool keeps moving, which does not touch doc 17's ladder).

**Acceptances:**
- 9-row eye rows 1/2/3/6/9 → **ACCEPTED** (sign-off table filled in
  game notes/NINE_ROW_ACCEPTANCE.md, two-tier basis noted; row 7 stays
  on the eyes-closed evidence card; ledgers #9/#14 exit condition met
  pending nothing further).
- Ecosystem map (doc 16) → **ACCEPTED** (header flipped; pointer
  blocks for all seven repos armed as part of the housekeeping batch;
  world_filler row truthed up in the same commit).
- world_filler integration plan (doc 17) → **ACCEPTED** (header
  flipped; approves the plan only, builds nothing).

**Go-items armed (engineering, in leverage order):**
1. Reactive battery re-baseline (all canonical proofs re-run
   --policy=reactive as the record; pretester expectations + handoff
   table flip).
2. Warden 575 [T] re-proof (EnemyDef + phase thresholds + damage
   schedule re-derivation + smoke pins + elite proofs + TTKBot).
3. export.ps1 dev/tester profiles + checklist step (M7 close).
4. WorldForge 49/50 merge per runbook → dusk re-export → game intake
   battery (the GO scopes assistant work in the WorldForge repo).
5. Docs truth-up (docs/03 stale body + INTERVIEW_STATE.md) + the seven
   pointer blocks.
All of 1–4 need exclusive Godot access (pretester lesson: concurrent
instances flake determinism) — the designer's play instance closes
first; runs start on their word.

**Evidence verified (assistant checks, not taps):**
- Assembler exporter commit `b7eae05f` → **CONFIRMED pushed** (fetchable
  from the checkout; branch also carries fresh 48x48 boss-direction
  pilot commits — the polished-sheets pipeline is visibly warming).
- CORE-34 no-ability clear → **CONFIRMED** via session.jsonl
  2026-07-29: three Warden kills (t556/581/721 across resets), ZERO
  ability events all session; the single player death was to a
  world_walk husk in a separate run. The M6 acceptance line is met on
  a shipped scenario (meet_yard_warden).
- itch page + devlog + Discord → designer-marked done; links requested
  for the tester-pipeline record (unverifiable from here until then).

**Still open (correctly, per two-tier):** the fresh-tier feel cards —
M2 formal close, six-ordinaries ratification, audio-in-normal-play —
plus eyes-closed audio evidence and the weekly cadences. These wait for
a rested (designer-clock) session by design.

## Addendum (~01:05) — tester pipeline verified live

itch page CONFIRMED: https://sarepat.itch.io/wildshot-adventures —
published 1 day ago, status In development, AI-assistance disclosure
set, TWO devlog posts (second ~01:00 tonight = GIF #2, the Warden
fight cut). Weekly GIF cadence: satisfied for this week; the deck
cadence card can be marked done (resets the 7-day cycle). Discord
link still to be recorded (no urgency). Designer note for the record:
they prefer a low-persona public surface — the pipeline design honors
that (game-forward posts, handle-only identity; the quiet-lab rule
actively benefits from dev distance).

## Addendum 2 (~01:20) — repo-sync batch executed; WF merge found DONE

Pointer blocks (per accepted doc 16) landed at the top of all seven
repos' agent-facing docs: game CLAUDE.md, this repo's docs/03,
WorldForge HANDOFF, world_filler clone HANDOFF, assembler README,
Resonance Forge CLAUDE.md, TileForge HANDOFF (repo = 'Semantic tile
generator design' folder; 'Forge' is the retired Sprite Forge).
DISCOVERY: WorldForge behaviors-49/50 were ALREADY MERGED (ae924e3,
full merge duty: 226 tests, goldens drift-free, both consumers, dusk
re-exported under v50 identity, flood unchanged 34556) — the deck GO
collapses to the game-side v50 intake, Godot-gated. Also: Resonance
Forge has its own 2026-07-29 handoff (G3 accepted) — audio moves in
parallel, designer-side. Stale docs truthed up: INTERVIEW_STATE
header now defers to the game tracker; doc 16 WF row corrected; 03
got a post-burn-down update note. Doc-16 rows for tileforge repo
location + Forge identity are worth a future nit pass.

## Addendum 3 (~01:35) — last-three chat pass + the cities directive

1. AUDIO DEFERRED (designer): both audio cards (eyes-closed + in-play)
wait for the Resonance Forge integration — placeholder cues will not
be separately ratified. AUDIO_CUE_MAP.md slot updated.
2. HOURS BACKFILL LOGGED (designer confirmed the evidence draft):
+5h36 Jul-26 interview, +3h10 Jul-27 planning, -3h55 Jul-28 sleep-gap
correction; net +4h51, marathon ~27h25. Honest in both directions.
3. GRASS SLITS: the ruling escalated. Designer (verbatim): 'we got to
fix this, else cities will litteraly be unplayable. like u cant walk
in most of cities'. Recorded as THE CITY-WALKABILITY DIRECTIVE — the
13-cell slit question is subsumed. Finding: the designer's own live
WorldForge arc IS the fix in progress (the-eight-holds v51-v57:
street web, line roads, city-lane bands, road-band art upstream ask;
WF handoff now behavior 57 / round 9 — it updated mid-session). The
dusk pack the game loads has NONE of it (styles are opt-in; dusk
recipe plain; flood unchanged 34556). PATH RECORDED: after the
round-9 verdict, either re-export dusk WITH the street styles or
swap world_walk to a styled town world; then game intake + designer
walk decides. Slits stay sealed meanwhile (13 invisible-wall cells;
not the real problem).
Still open: Discord link; the Godot-gated chain awaits game close.

## Addendum 4 (~01:50) — THE CITIES PROBLEM, finally diagnosed WITH the designer

Correcting addendum 3's framing after the designer demonstrated on video
(GIF dump_61148) + confirmed 'when i stop in the video while walking
thats cause i cant move further'. THE REAL PROBLEM: in the west city
(settlement 1, anchor 94,128) the gaps between house footprints neck
to ONE walkable cell and consecutive necks are STAGGERED (offset) —
the player body (0.5 dia) fits a cell exactly, so every jog = walking
into solid = DEAD STOP. Corner slip cannot help (the deflection cell
is solid too). Connectivity audits all pass (57.7% walkable, all
entrances reachable) because the metric that matters is PASSAGE WIDTH
+ STAGGER along walking lines, not connectivity. The spawn/harbor
city has 2-3 wide streets and walks fine — which hid the problem from
the earlier region diag. The designer has been fighting exactly this
all along (their WF narrow-streets/line-roads/street-web arc IS this
campaign); the assistant repeatedly misread it as slits/connectivity.
UPSTREAM ASK (WorldForge, recorded here, needs designer word to
execute): a minimum-passage rule in the pack export — every
inter-footprint gap either >=2 walkable cells wide OR fully sealed;
no 1-wide staggered teases. Alternatives: enable the street-web style
on the played world's recipe, or swap world_walk to a styled world
post-round-9. Game-side stays hands-off (collision = walkability
verbatim by contract).

## Addendum 5 (~02:15) — the engineering chain executed

Designer closed the game and said go (~01:56). In one exclusive-access
block:

1. REACTIVE RE-BASELINE (game 4b1c2b4): dodge_proof defaults to
reactive; unsuffixed reports are the record; 25-row battery
regenerated — the three former wolf-pair FAILs PASS under the record,
the geometric MUST-FAIL canary still fails, and the primary model
stays watched via three [primary] MUST-FAIL baseline rows + committed
_primary evidence (a primary PASS would mean the sim changed). Stale
_reactive evidence retired. Ledger #11 execution complete.
2. WARDEN 575 (game 59cd087; planning 0c6c04f): hp 575, floors stay
66/33 pct, proof schedules re-derived on the same skeleton
(transitions t1207/t2413, kill t3301), smoke pins tick-exact at the
new crossings (379/189), all elite proofs + meet PASS, TTKBot 21/21
EXACT — Longbolt vs Warden 11.88 s (was 8.38; design line ~13).
3. EXPORT PIPELINE (M7's last item): presets dev/tester (feature tag
'tester'), main.gd ONE-FLAG dev_tools gate (console/god/slow-mo/
verdict, free speed steps, bot+audit CLI; speed presets stay),
build_info stamp via git describe (tester HUD shows it),
tools/export.ps1 export->loose-packs->zip->boot-check->butler-prep,
WorldforgePack.resolve_src exe-relative fallback. PROVEN: both
artifacts boot exit 0; the tester exe rendered the full dusk world
from LOOSE pack files beside the exe (8 hashes verified, 170k
placements). Loose packs mean the incoming passage-rule pack can
swap into a built zip without re-export. pretester_check gained the
export step (TODO retired).

LESSON (recorded): never edit sim data while a battery runs — the yw
rows landed post-edit by timing luck; sequence edits between runs.
Final ALL-GREEN pretester (incl. export step) running; M7 closes on
it. Remaining game-side: the WF passage-rule pack intake when the
designer's WF agent ships.

## Addendum 6 (~02:30) — passage-rule pack intaken; M7 acceptance running

The designer's WF agent shipped the two-wide rule (WF 7b08f35: inside
settlement interiors, walkable plain ground not part of a fully-open
2x2 block seals, cascaded to fixpoint; four false-positive classes
scoped out during landing — interiors are built fabric not radius
boxes, trees don't trigger pinches, seals don't breed seals, and
CONNECTIVITY OUTRANKS WIDTH: ~60 sole-access corridors reconciliation-
reopened + flagged for blocked-ground dressing; hard audit gate in the
exporter + byte regression test; 228 WF tests green). GAME INTAKE
EXECUTED per the runbook (game 3b5ac3d): validator + boot green (solid
29175 -> 29301, +126 = the ~123 seals; flood 34556 -> 34433), porosity
diag still exactly 11, smoke green, west-city diag grid confirms
slaloms sealed / 2-wide streets open, world_walk composition re-proven
PASS 0 hits x3 seeds. Export seam committed (2a0e907) with loose-pack
shipping - this drop swaps into built tester zips without re-export.
Final full pretester (M7-closing acceptance) running. PENDING: the
designer's walk on the (94,128) west-city street from their video -
the human acceptance of the whole cities arc.

## Addendum 7 (~03:15) — THE CITIES ARC CLOSES: designer walks between houses

The full resolution, both halves same-night:

1. GAME HALF: ActorSortSpace at the ACTORS band — actors + world
verticals (structures/props/fence/wall) y-sort together. v1 (alt-tile
sort origins) DROPPED ROOFS — designer caught it on sight; v2 renders
each connected structure component as its own mini TileMapLayer
anchored at its base-row bottom edge, sorted as one node (5a84002,
designer-eyeballed). Lesson: headless boots cannot see render bugs —
designer eyes are the render gate.
2. WF HALF (designer's WF agent, via relayed prompt): WYSIWYG
walkability (their 91d1fa9) — ALL seal machinery deleted (-428 lines:
slit seals, back-pocket seals, reconciliation, two-wide rule + its
audit). Honest core: collision = art outline minus each structure
type's declared pass cells; hard bidirectional gate (ground render <=>
walkable); WYSIWYG_EXCEPTIONS ships EMPTY. Dusk re-export flood 34739
(+306): 481 one-wide inter-house strips walk, 474 reachable, 7
enclosed courtyard islands legal. Also behavior 60 harbor docks
(eight-holds; round-12 verdict = designer, WF-side).
3. INTAKE: boot + hash chain green; porosity diag re-pinned 11 -> 44
(33 gateway pass cells — cave mouths/mine shafts/dens/crypts/gates —
spot-verified as 2-wide ss gateways; per-drop pin doctrine written
into the tool); world_walk re-proven PASS 0 hits x3 seeds.
4. ACCEPTANCE (in play, two-tier): 'hallelujah! finnaly works' — the
designer walks the grass strips between houses. The arc that began as
'13 sealed grass slit cells' ends with what-you-see-is-where-you-walk.

NEW STANDING RULE (designer, verbatim intent): game-repo sessions do
not execute other repos' plans — upstream needs become recorded asks
or prompts for that repo's own agent (the pattern that shipped both WF
commits tonight). Added to the game CLAUDE.md Authority section +
assistant memory. Final ALL-GREEN pretester running as the seam close.

## Addendum 8 (~03:30) — account-switch handoff written; session closes

Cold-start HANDOFF.md rewritten at the marathon seam (game 7854bda):
full current state + 17 gotchas incl. tonight's lessons. Ledger swept
(#9/#14 closed by the 9-row sign-off; #15 closed by the WYSIWYG arc —
ledger now fully clean or Phase-C deferred). DESIGNER_QUEUE marked
historical (the deck is the living queue); export design + intake
runbook era-stamped. Designer is mid-iteration on eight-holds round
12 (docks) in WF — live designer territory, lane rule applies. Next
session cold-start path: game CLAUDE.md (auto) -> notes/HANDOFF.md.
Clock stopped 03:26; the designer switches accounts here.

## Addendum 9 (~03:35) — projectile research review (designer's last-usage read)

Designer shared two bullet-hell architecture docs (deep-research +
a distilled recommendation). VERDICT: they describe the architecture
Wildshot already ships — central manager, behavior-as-data motion,
mathematical circle collision, per-pattern MultiMesh atlas rendering,
honest hitboxes, fire-events+seeds networking model. We exceed them on
determinism (goldens/hashes), hitbox honesty (exact-fit orbs = art
contract), and banding (Laws vs additive-groups). Deliberate
deviations stand: 60Hz pinned (determinism contract; their 120Hz tip
rejected), spatial hash stays deferred-by-measurement (ledger #6).
THREE PARKED NUGGETS: (1) CHECK projectile_view seeds prev=curr on
slot reuse (one-frame streak risk on recycled MMI slots — headless
proofs cannot see it; cheap check next session); (2) one-shot
MultiMesh buffer write = the scale lever if live counts ever 10x
(post-Gate-1); (3) region-split MMIs for culling when combat roams
world_filler-scale maps (post-Gate-1). No action tonight.

## Addendum 10 (~03:40) — projectile review #2 absorbed (the sphere-set critique)

Designer shared an external review of our CURRENT projectiles (video-
based). VERDICT: keep + build on — independently validates the sphere
adoption + small-shot retune. COMPLIANCE CHECK against the shipped
game: (1) their proposed rule 'everything inside the hard outline
collides' IS our shipped exact-fit/full-bleed doctrine — rim in
hitbox, visual==hitbox; (2) 'soft marble' white orb: GAME EXONERATED
— project default_texture_filter=0 (nearest) AND projectile_view
forces TEXTURE_FILTER_NEAREST on MMIs; softness = the generator's
deliberate shading + their video compression. Sharpening (2-3 tones,
binary edges) = sphere-generator tweak, designer tool territory (lane
rule), zero-code re-import. PARKED: (a) designer stress-density look
re the 1px-smaller [T] question (audit_density rig exists — this IS
the [T] test-gate); (b) M-FX v2 candidates grow: 2-3 frame internal
pulse + compact-trail-for-fast-shots (projectile_view would need
frame support IF pack v2 ships frames); (c) THE SILHOUETTE-LANGUAGE
ROADMAP — their shape table maps onto the role grammar (dart=
Leadshot, ring=zones done, large-orb=heavy done, crescent/spiked=
future roles) and would deepen Law-3 (shape-first, never color
alone); proposal for the sphere generator v2, designer-authored.
Nothing built; all designer-triggerable.
