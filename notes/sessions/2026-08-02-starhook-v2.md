# 2026-08-02 — STARHOOK v2: the prototype merge (sl-0115)

Game repo session, hands-free, routed by sl-0115 as amended. Read
order honored: `notes/reference/starhook-proto2/INDEX.md` FIRST (the
corrections block treated as law over everything older), then
DESIGN/TUNING/HANDOFF, then the extracted prototype build's
`engine.js`, then frames pulled from both clips (044051 @ 4/12/24/38 s,
044205 @ 6/15 s) — the look was built against the designer's own
pixels. Game commit: `0a71a6b` (SERIAL 21→22; WSR stays v2).

## What the seam built (shape per the prototype; every number [T])

- **Instant cast**: interact (F) at a land portal casts NOW — the
  cast IS the aggro. The stillness cast retired; foraging's
  stillness verb untouched. CAST_COMPLETE carries rarity + biome.
- **THE PULL as a real .tres** (`data/rift_pull.tres`): 1.35 t/s,
  ±0.45 rad oscillation at 0.004 rad/tick (~26 s), player ×1.0 /
  catch ×0.3 / HOSTILE shots ×0.15. Friendly bolts fly true — the
  prototype's own choice and CORE-32's. Enemies drift only in
  moving states: Law 4 (telegraphs stay put) outranks the
  prototype's drift-while-telegraphing — recorded deviation.
- **Line stability = HP, THREE hard lives** (deck tap shk1loss):
  drains land through THE damage path as pattern −5 (Law 8 — every
  snap explainable): passive 0.4/s = 1 hp per exactly 150 ticks
  (integer accumulator, 1/600-hp units), deep-edge +2.2/s inside
  the 0.8 t strip. Snap = life burned + full re-spool + 90 t grace
  + LINE_SNAPPED; the THIRD snap is the normal dive-lost death
  (wake at the shore; hardcore never staked). Hit grace 30 t
  (prototype 15 @ 30 t/s, converted) blocks bullets only — the
  drains never pause.
- **The win is purely the kill** (correction #7 — the reel is CUT):
  the kill banks gold directly (no ground drops in rifts), draws
  the biome fish (rng_loot, 45/35/20 weighted commons; the rare
  rarity always lands the biome's rare species), emits
  CATCH_LANDED, and the dive auto-ends after a 150 t linger. The
  mouth door stays as the flee path.
- **Fish are species-currency** (deck tap shk2ctch): persisted
  per-species in the profile (`starhook_fish{}`) from this seam on.
- **Three biomes** as pattern-variant .tres — void ring 12×30° @
  3.4 t/s, comet spray 6.3 t/s + dart cd ×0.8 — SAME pattern ids,
  SAME leads (one hostile language; the lead law holds by
  construction). Four new defs at roster 26–29 (append-only), six
  `rift_<biome>_<rarity>` scenarios supersede rift_common/rare.
  Authored slice nodes carry biomes (0,1,2 cycled [T]); the portal
  shows its biome before the cast.
- **Ambient rifts** (deck tap shk3spwn): interval/chance/cap/ring
  in `balance_frame.json starhook.ambient` [T]; rng_misc's FIRST
  consumer; ambient nodes are serialized state, consumed away.
  Slice-only opt-in (`rift_ambient` on the scenario) — every proof
  world inert by construction.
- **Four rods**: Cane/Splitwillow stand as built (confirmed rows);
  Heavyline (88 t, 16 dmg, 9 t/s) + Twinreed (28 t, 2×±3°, 3 dmg,
  15 t/s) converted 30→60 t/s per HANDOFF §7. Unlocks 1/3/5/8 [T].
  **R swaps** (the designer's word): replay_save moved R→J
  (no-F-row law holds; hints self-document); the swap rides the
  already-recorded weapon_select byte — ZERO replay-format change;
  locked selects refused sim-side; the equipped rod persists by id.
  **Heavyline's prototype hitstop+shake did NOT ride — CORE-32 bans
  routine hit-stop/shake** (constraint outranks prototype flavor,
  same class as the 4.2 overworld speed not riding).
- **The 50/50 split + the galaxy view**: world capture LEFT (the
  body at the rift; the line drawn INTO the world portal + portal
  pulse — the living overlay), galaxy RIGHT under a FIXED camera:
  the new 12×13 arena's 10×11 interior fills the right pane 1:1 at
  base scale (walls sit exactly off-pane; bullets die at the pane
  edge like the prototype's cull) — Law 1 by construction. THE
  BAIT FIGHTER is the star (corrections #1/#2): an 11×12 code-drawn
  micro sprite with a glow pad; the catch renders the prototype's
  20×12 star-fish pixel map, biome palettes verbatim, gold when
  rare. Backdrop gradient/wisps/stars, rim, deep-edge shimmer, flow
  arrows, biome stubs — per-biome, seeded per run, view-only. The
  line: sag↔taut, red under strain, travelling spark, always INTO
  the world rift and OUT of the galaxy rift (correction #4), world
  canvas z 40 so hostile bands stay above. Overworld portals render
  AT LAST (they never had a view) with the biome name + "[F] cast
  the starhook" prompt.

## Proofs, gates, evidence

- DodgeBot models the pull (self, INCLUDING the stay candidate —
  a still body is not still) + hostile-shot drift in projection +
  the deep strip as a positioning keep-out (never a survival
  threat: CORE-33 is about attacks, and dodge_proof now counts
  bullet hits only — the drain is the session clock).
- **All four rift rows PASS floor+cap, seeds 1,2,3 × 3600, FIRST
  RUN in the new room with the pull aboard** — margins 0.120–0.122;
  comet floor 0.079 (the tightest, honest tier-3-watch). Battery
  41→43 rows / 83 runs. Goldens re-recorded + verified ×10.
- gather_test REBUILT for the v2 contracts (instant cast, ambient
  determinism, pull/drain exactness, grace, snap ×3, post-win
  clear, fish banking, rod gating, loader refusals for broken pull
  defs + malformed biome tables — negative-tested).
- Windowed evidence committed + read by eyes:
  `reports/rift_galaxy_audit_{base,desktop}.png` (the two mandated
  scales; integer viewport stretch makes desktop pixel-exact 3× by
  construction — recorded).
- Full pretester: LAUNCHED at seam close on the exclusive seam —
  result recorded in the completion sync-log entry.

## One real find while proving

The first rift proof run "FAILED" 97 hits seed-invariant from t149 —
the probe showed ZERO real hits: dodge_proof counted every
DAMAGE_APPLIED as a hit, so the passive drain's first 1-hp chunk (at
exactly tick 149, accumulator math) read as dodge failure. The fix
is the honest accounting above (drain ≠ attack). Also recorded as
game handoff gotcha #35: an improvised PowerShell gate loop reported
nine GREEN gates as all-FAIL (`$LASTEXITCODE` unreliable in wrapped
loops) — diagnose surprising all-reds by re-running ONE test with
output visible; use bash for quick sweeps.

## Not this seam (by routing/corrections)

Gear seam (rods + simple equipment DROPPING from starhook bosses,
level-gated — NEXT after the soul, correction #5, own routing);
the small behavior-first skill tree (post-class-trees); node
drift/despawn; vendors (fish-priced goods are a future chapter's
system); water fishing PARKED by correction #3 (revive only on the
designer's say). Catch-card length flavor + the divider's
travelling spark = refinement-round material.

## Open for the designer (refinement rounds + Green days — all [T])

The cast feel, pull strength/oscillation, drain rates, the three
lives' generosity, rod cadences + unlock levels, R-swap feel,
ambient cadence, biome twists, the bait fighter's size, the split
look at both scales (evidence PNGs committed), fish names in the
toasts, the six picker rows.

Concurrent planning session noted: sl-0116..0122 landed while this
seam ran (Green-days paper batch #1 incl. the sl-0121 quest-icon ask
and sl-0122 boss-sprite ask targeting game) — none absorbed here;
they route on their own.


---

# ADDENDUM — the refinement pass (sl-0123 + sl-0125, same day ~15:15–16:30)

Two view/feel seams routed mid-day after the designer cast, landed as
separate sealed seams.

## sl-0123 — THE DRAG IS CUT (game commit `16315e6`)

Arena combat in the galaxy view is NORMAL combat — no movement drag
on the bait fighter, the catch, or any shot. The rift's pull lives in
THE LINE ONLY: strain clock, deep-edge strip, bullet strain, visual
tension. The rift_pull entity-drag system retired CLEAN:
`data/rift_pull.*` superseded by `data/rift_line.*` (drains / deep
edge / lives / graces only), `SimWorld.rift_line` /
`ScenarioDef.rift_line` renamed through every consumer and all ten
rift scenario files; the movement/enemy/projectile integrators and
DodgeBot's candidate walks + threat projection carry zero pull code;
the sl-0115 hostile-only-drift and Law-4-drift deviations are moot.
Deep edge stays [T] (drain + bot keep-out + shimmer). Flow arrows
DROPPED [T call] — they advertised a current that no longer moves
anything. gather_test PINS the absence (still fighter never moves; no
shot of either faction drifts). The never-bind pin is mechanized: the
icon pack's item.unique.undertow glyph (a retired word) reds the
wiring gate on any game/ui/input/sim/data reference; the glyph purges
at the next icons release upstream. All eight rift lanes re-recorded
PASS (margins 0.120–0.123; the comet floor's tight 0.079 relaxed to
0.121 — the current was the squeeze); goldens x10. Evidence captured
FOR REAL: the desktop PNG is a screen crop of the presented window —
the first attempt caught a bystander window (the game hadn't taken
the foreground); it was deleted unshipped, the probe now forces the
window topmost and refuses crops that don't look like the galaxy.
Full gate BEFORE commit per gotcha-32: 30 fixed + 83/83 battery
on-matrix; the one red was the reports byte-gate listing exactly the
commit's ten re-baselined files.

## sl-0125 — THE SPLIT RATIO GOES [T] (view-only, zero sim bytes)

The world/galaxy split is a LIVE-FLIPPABLE options cycle row ("rift
split": half & half / two-thirds galaxy; both profiles, [ui]
persisted + settings-gate round-trip) — my [T] call for the
options-row form so the designer flips it in play. THE ARENA CELLS
STAY IDENTICAL: main re-anchors the panes and re-fits the fixed
camera (half = 1.0 zoom, byte-same as built; two-thirds = 1.0227
height-fit, interior centered — Law 1 by construction at either
ratio); the world pane keeps line/body/portal readable at 1/3 width;
the galaxy backdrop grew a 1-tile skirt so the fitted camera never
shows void. ZERO SIM BYTES: smoke hashes byte-identical to the
sl-0123 record (f98c9435b3f0bb8d / 336b9414e9a9ca04). EVIDENCE: four
REAL distinct captures, two scales per ratio
(rift_galaxy_audit_{half,twothirds}_{base,desktop}.png) — the
evidence-twins debt discharged; the sl-0115 pair retired. The
designer flips and rules; the INDEX amends on the pick.

Push of main follows the sl-0125 gate (planning's GitHub verification
was pending on the stack).
