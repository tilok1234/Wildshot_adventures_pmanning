# PASTE E — DRAFT (GATED: fires only on the designer's post-play word; sl-0213)

> Drafted 2026-08-05 while the designer was at work, so tonight is
> drop-and-go. NOT YET ROUTED — the designer plays the reshaped
> build first, then their word fires this (possibly reshaped by
> what play teaches). The seal warm-up (item 0) rides whichever
> session runs first once the stray Godot window is closed.

```text
-> GAME repo (fresh game session; PASTE E - THE CLOSE-FIGHTER
ROUND; a SIM seam - full gate before every commit, goldens
re-record EXPECTED (overworld combat behavior shifts, unlike the
leash's byte-identical luck); every number/pattern [T]; NO feel
verdicts; wave-split by family sanctioned - waves resolve honestly;
resolve sl-0213 at close; hours bracket the session)

0. THE SEAL WARM-UP (rides free): the sl-0229 formal full-pretester
   seal on the current tree - the entry guard clears once the idle
   Godot Project Manager is closed; run it, record it, then start.

1. THE VOCABULARY IN DATA: enemy defs gain a preferred-engagement
   identity (the sl-0213 ruling: "melee" names DISTANCE, never
   weapon type) - range bands [T] (close / mid / far or the seat's
   cleaner cut), derived from existing behavior where possible.
   Doc-23 vocabulary-layer ruling 1 is the law text.

2. THE CLOSE-FIGHTER RE-ARM: every slash-class family (wolves,
   goblins, boars - the seat enumerates the exact roster; ~half of
   Green) keeps its CLOSE-PRESSURE role but fights with SHORT-RANGE
   PROJECTILES: telegraphed, honest-dodgeable, fired from close
   range with real gaps (point-blank fans / rings / lunge-shots -
   composition per family, identity preserved). The slash arc
   either retires per family or becomes a TRUE projectile pattern
   (visible, hitbox-covered per Law 2/8 - no invisible melee
   boxes). The designer's density rail, verbatim: "like dont get me
   wrong and over do it, but atm their projectiles are mostly
   nothing at all its just that they run after u that u notice" -
   close fighters contribute a MINORITY of screen bullets; per-
   family bullet budgets [T]; readability sanity (strobe + a
   worst-case density screenshot).

3. THE MELEE-HARMLESS PINS RETIRE HONESTLY: the sl-0208 finding
   (wolf 4-of-63 swings at hurtbox 0.175) re-derives - a STANDING
   player must be genuinely threatened by close fighters again
   (the fairness frame's other half); the DodgeBot floor/cap
   proofs extend to every new close pattern; deaths explainable,
   the floor absolute.

4. RAILS: zero recorded-format growth expected (defs + patterns
   are data; report if otherwise - any SERIAL bump truths the lock
   at planning's sweep); goldens re-record deliberately under full
   re-baseline (overworld recorded scenarios WILL shift); the
   designer plays before refinement pastes; resolve sl-0213 at
   close with per-family findings named.
```

## Open shape points the designer's post-play word can adjust
- Which families go first (a wave-1 pick of 2-3 vs all-at-once).
- Whether any family keeps a true contact attack as its identity
  (the ruling allows it if the attack becomes an honest visible
  projectile-class hitbox).
- The bullet-budget feel ("dont overdo it" is the only word so
  far - the first wave calibrates it).
