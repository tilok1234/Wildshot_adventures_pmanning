# Session notes — 2026-07-27

## External review → maintenance commits (designer-approved)

- The designer requested an external read-only review of the plan (devil's-advocate
  pass + critique) in a separate Claude session, then approved its three leverage
  fixes for direct commit: add the missing numbers dimension, re-tag feel-dependent
  locks with falsifiers, and open a tester/market pipeline.
- Branch: `plan-fixes-2026-07-27` off `claude/questionnaire-note-taking-9vl2sl`.

## Commits in this batch

1. **Five stale-state desyncs fixed** — register "do not infer as decided" tail
   (still listed engine/team size/first-journey/death rules as open after
   CORE-20/17/43); GDD Appendix A pre-completion bullets; GDD §18 pet
   passive-benefits leftover; risk register header (CORE-32 → CORE-55);
   interview-state status table (CORE-17–20 listed as open above the ALL ANSWERED
   row). All five dated to the same day the 2026-07-26 sweep declared "all
   documents agree" — evidence for the standing mirror-maintenance concern below.
2. **CORE-33 falsifier** ([L] → [L/T]): lab-gated reopening conditions and a
   fallback menu, recorded in register, GDD §8, questionnaire, and open questions.
3. **Phase D falsifiers**: no-pity baseline (inner no-guarantees tag [L] → [P/T];
   dry-streak feel test at simulated p95 unluck) and the committed-instance rule
   (interruption/abandon tests must read as known commitment, not lost progress).
4. **Pre-registered effort model + scope menu** in the roadmap: slice ~610–1,400 h;
   full game as specced ~4,500–9,000 h (≈9–17 years at the 10 h/week planning
   anchor). Scope menu recorded as a pending designer decision.
5. **Gate-failure consequences pre-registered** for Gates 1 and 2 (register +
   roadmap gate pointers).
6. **Tester and market pipeline** added to the roadmap; referenced from the risk
   register's market runner-up row.

## Pending designer inputs (these block honest Gate 2 evaluation)

- PROD-01: the reliable weekly floor — the hours that survive a bad month; every
  year-figure in the effort model divides by this.
- PROD-03: cash budget (Steam fee, subscriptions, music/SFX, capsule art,
  contingency).
- The scope-menu choice: slice-as-v1.0 / shrink the bill / raise capacity /
  accept the long horizon in writing.

## Deliberately NOT done (open structural decision for the designer)

- Register-as-single-canonical-record and de-duplicating the register's
  cross-system rules list: a workflow decision, not a mechanical fix. The five
  desyncs above are the argument for making it soon.
- No questionnaire PROD answer slots were filled — drafts await the designer's
  real numbers.

## Review recommendation on next steps

Lab before further Part II paper: every pre-lab answer deepens lock-in against
lab evidence. Recorded as a note in the handoff's next-steps list; the choice
remains the designer's.

## Evening session (designer's PC) — PROD-01

- Designer switched to PC as planned; repo cloned fresh into
  `Documents/Wildshot_adventure_final_planning` (git identity = repo owner).
- **PROD-01 answered and approved:** Wildshot is the full-time occupation.
  Scheduled 72 h/week (5×8 weekday + 2×16 weekend); **reliable planning floor
  40 h/week** (weekday core). Assistant pushed back on planning against the
  72-hour ceiling; floor-vs-ceiling split approved by the designer.
  Self-correcting rule: hours logged from Phase A day one; a 4-week rolling
  average below 40 resets the floor and re-derives the roadmap.
- CORE-20's 10–40 h/week band amended across questionnaire, decision register,
  GDD §15, roadmap, risk register, and handoff (it predated full-time status).
- Roadmap re-anchored: slice → Gate 2 ≈ **4–8.5 months** at the floor
  (pre-registration had assumed 10 h/week → 1.2–2.7 years); full game as
  specced ≈ **2.25–4.5 years** at the floor.

## Evening session — CORE-20 asset status verified (showcase packet)

- Designer shared the full asset packet: four TileForge theme packages +
  reference pack (from `Documents/Semantic tile generator design/exports/`)
  and the new **Actor Forge Sprite System** pack.
- Verified by direct inspection (sheets viewed frame-by-frame; Crownhold
  flagship + cave-dusk scene reviewed at zoom):
  - **TileForge**: shipping system — 31,431 tiles × 80 families per theme,
    deterministic regeneration, Godot importers, pixel-match acceptance test,
    13 biome scenes incl. dungeon-room/cave/corrupt-zone; quiet-ground law
    visibly encoded (Crownhold reads calm at map zoom).
  - **Actor Forge v2.3.0**: 32×32, 4 facings, 23-frame contract
    (idle/walk/attack/hit/cast/death), baked ink outline, foot pivot,
    theme-matched palettes, deterministic recipes; 12 actors × 4 themes;
    first quadruped (wolf) just landed — side profiles strong, front/back
    facings noted as the usual 32px quadruped squeeze (WIP).
  - Roster already maps to the pack role grammar: wolves = chasers,
    caster/cultist = ranged, skeleton/zombie = fodder.
- **Identified gap → pre-registered as the pre-lab asset task (designer
  committed):** the combat-effects vocabulary — projectile shapes (player +
  one hostile family language), telegraphs, ground-hazard markers, hit
  effects, boss-cast presentation — authored against the eight laws.
  Recorded in roadmap Stage 2, register CORE-20 row, GDD §15, questionnaire
  CORE-20, and the handoff.
- Next: PROD-03 cash budget, then the scope-menu choice.

## Evening session — PROD-03

- **PROD-03 answered and approved:** cash is not a project constraint. AI subs
  ample; Steam fee trivial; music/SFX self-produced (audio pipeline planning
  started today); store/capsule art self-produced. No ceiling needed.
- **Assistant addition (approved):** the audio pipeline inherits the forge
  rule — readability Law 7 (audio as the eyes-closed second channel) and the
  CORE-50 audio baseline (separate channels, key threats audible) must be
  encoded from the start, recorded before the pipeline is designed.
- **Sustainability assumption recorded (approved) with falsifier:** full-time
  capacity is financially sustainable across the 2.25–4.5-year horizon; if
  income pressure ever imposes a de-facto deadline, PROD-01 and the scope
  menu re-open by rule (no personal numbers recorded — assumption only).
- Remaining input: the scope-menu choice (last of the three).

## Evening session — PROD-01 amendment (day job correction)

- Follow-up statements ("while im at work", "6 weeks vacation") surfaced that a
  **day job exists** — the "full-time occupation" phrasing recorded hours earlier
  was wrong. Designer asked not to over-weight it ("big nerd, can go hard");
  assistant corrected the record without re-litigating the numbers.
- PROD-01 amended everywhere: day job on record; ≈72 h/week ceiling as stated;
  **floor 40 kept explicitly as a claim under test** (self-correcting rule is the
  honesty mechanism); realistic-case note added (measured ~20 h/week → slice ≈
  8–17 months, still inside the accepted horizon and the ≤5-year rule).
- PROD-03 sustainability reworded: employment income backs the horizon (no
  runway pressure); watched risk shifts from money to energy.

## Evening session — scope-menu decision (closes all three blockers)

- Designer challenged the effort model ("what is gonna take us 7 years?");
  assistant decomposed where the hours live: feel iteration, encounter
  authorship under the handcrafted rule, tuning/simulation, human playtesting
  cycles, integration tax — assets were already assumed near-free. Also
  conceded honestly: the forges exceed the model's assumptions, and Phase A
  measurement replaces the debate within weeks (~60 h optimistic = ~1.5 weeks
  at the floor).
- **Option 4 approved** ("ye i think we can do 4. so i wanna keep everything
  we planned ofc"): full game as specced stays the target; horizon accepted
  in writing. **Gate 2 "viable" defined:** remaining bill at measured slice
  velocity ≤ 5 years at the then-current PROD-01 floor; longer auto-triggers
  slice-as-v1.0 → shrink-the-bill (capacity-raise moot). Recorded in roadmap
  (scope menu + Stage 9 gate), register (gate-consequences + do-not-infer +
  header), GDD §15, open questions, handoff, and this state file.
- **Designer flagged intent (not yet recorded as a decision):** an AI-driven
  bot system for "boring testing" — soak/regression runs while the designer
  is away. Assistant to propose recording with the gate guard (bots never
  substitute for the human outside-tester gates). Also surfaced a PROD-01
  accuracy question: designer said "while im at work" — clarify whether a
  day job exists alongside the recorded full-time status.

## Evening session — pre-registration batch (designer approved all four)

- Batch approved via structured prompt: (1) PROD-01 day-job amendment (own
  commit, above), (2) vacation Phase A window, (3) bot-testing system,
  (4) WorldForge + handcrafted contract. Items 2–4 in this commit.
- **Vacation block:** ~6 free weeks from ≈ early Oct 2026; stated capacity up
  to 16 h/day (≈670 h ceiling, 4× Phase A pessimistic). Targets two Gate 1
  cycles; tester turnaround is the constraint; fresh-hands guard on feel
  verdicts. Pre-vacation evenings: effects pack, audio pipeline, Godot +
  TileForge scaffolding via §4 acceptance test, itch/devlog channel.
- **Bot system:** mechanical verification only (lowest-speed dodgeability =
  CORE-53 mechanized, soak/regression, percentile sims, TTK); never gate
  judgment. Added to roadmap Stage 2, prototype-spec instrumentation, and the
  register's new Tooling contracts section.
- **WorldForge revealed** (third forge, WIP, completion committed): 256×256
  worlds from TileForge packages (sourceCommit-locked), settlements, tiered
  routes (highway/street/trail), fords/bridges, discoveries layer, read-only
  viewer by contract; elevation/cliffs a later milestone. Recorded with the
  handcrafted-rule contract: drafts, not authored geography; curation is the
  authoring pass; progression-critical placement always hand-decided.
  Assistant stress-test observation passed to designer: trails #15–#17
  duplicate endpoint pairs already connected by streets/highways — either
  RD7 backtrail doctrine working or a route-dedup case.
- **Same night, ~1:50 AM:** WorldForge elevation adapter v4 landed — rock
  masses render as terraced peaks, walkable cells stay level 0 (elevation is
  presentation; gameplay stays flat — the right scoping pre-lab). Register
  entry updated to match.

## Late evening — Phase A lab build plan (ultracode workflow) — APPROVED

- Designer asked whether to plan more concretely before starting the lab and
  whether to use single-agent max or ultracode; assistant's honest call: one
  tight build-plan artifact via multi-agent (architecture one-way-doors
  deserve independent proposals + adversarial verification), then back to
  single-agent for the build. Designer opted in.
- Workflow: 11 agents / 6 phases — 2 constraint extractors (52 binding
  constraints), 3 independent architecture proposals (determinism-first /
  feel-first / architecture-first), judge synthesis, drafter, 3 adversarial
  verifiers (38 findings, 15 blocker/major, all applied), finalizer.
  ~832k tokens, ~30 min.
- Result: `docs/12-PHASE_A_LAB_BUILD_PLAN.md` (357 lines). Key decisions:
  pure sim core @ fixed 60 Hz with replay+hashing from week 1; no Godot
  physics (custom SoA collision, M2 stress-rig escape hatch); zero-RNG
  player fire path; three-tier DodgeBot + human lowest-speed confirmation;
  two-profile builds; 12 milestones (9 pre-vacation incl. M-FX effects
  track, M8 + two Gate 1 cycles in vacation) with slip ladder.
- Engine pin corrected to the verified install: Godot 4.6.2 stable at
  ~/bin/godot (plan drafted 4.3.x; 4.6.2 satisfies the importer floor).
- **Designer approved the plan**; six flagged rulings stay open (sprites in
  lab, Longbolt cap, slip order, M5 effects deadline, Blast Rune, tester
  recruitment sizing) — ruled on as they come due.
- **Designer deliberately deferred starting M0** ("lets not start just yet")
  — but created the game repo minutes later:
  https://github.com/tilok1234/Wildshot-Adventures (empty, awaiting M0
  scaffolding; public for now, designer plans to flip it private — assistant
  noted private repos are free on GitHub's free plan). Next session can open
  with M0 whenever the designer says go.
- **CI addendum approved** and added to docs/12: staged GitHub Actions jobs
  (M0 lint → M1 pixel-match → M4 golden-replay hash gate → M7 nightly
  DodgeBot → M8 CI-only tester exports). Replay/bot jobs pinned to Windows
  runners per the §2.4 determinism scope; self-hosted runner on the dev
  machine as the M7-era switch; lint may run on Linux.

## Late night — M0 EXECUTED (build phase started)

- Designer gave the go after reviewing what M0 contains. Game repo cloned to
  `Documents/Wildshot-Adventures`, branch `main`.
- Landed per docs/12 §4 M0 + §5 (commits 2286ad0 → 92dfc7f):
  - **CLAUDE.md contract**: authority rule (planning repo is design authority;
    conflicts stop-and-flag), binding-constraint digest (CORE-31/32/33/34/35/
    36/40/44/50/51/53/55, GDD-16, SPEC-A), no-RNG rule, determinism scope,
    quiet-lab + CORE-54 evidence rule, fresh-hands rule, scope tripwire,
    hours-log rule, weekly GIF cadence, engine pin, milestone tracker.
  - **Godot 4.6.2 pinned** (project.godot + README + CLAUDE.md); project boots
    headless in the verified install, exit 0.
  - **Directory skeleton** per §2.5 (+ `reports/`); empty `game/main.tscn`;
    640×360 viewport/integer-scale/Nearest project settings per §2.9.
  - **Hours tooling live before the first code commit** (PROD-01):
    `hourslog.ps1` + `hours_report.ps1`; first real entry logged (M0 session
    start); report runs and correctly notes the first rolling window closes at
    end of week 4.
  - **CI green on first push**: banned-RNG grep under `sim/` + gdformat check
    (Linux runner — allowed for lint per the CI addendum). All three runs green.
  - **TECH_DEBT_LEDGER.md** opened with the seven named seed entries.
  - Fix commits: `.gdignore` on notes/tools/reports (Godot was importing
    hours.csv as a translation asset) + a .gitignore exception for it.
- **M0 acceptance met**: repo boots an empty window (headless verified), hours
  log has entries, report runs against them, ledger committed.
- **Next: M1** — TileForge importer + §4 pixel-match acceptance test green in
  the game project + greybox arena from the dungeon-room set. Integration path
  per handoff: theme zip → GAME-GUIDE.md → prove the renderer against
  `map-reference.png` before anything else.
- Open loose end: designer runs `tools/hourslog.ps1 stop` when tonight's
  session actually ends.

## Late night — Sprite Forge reveal + asset drops committed (designer-approved batch)

- Designer shared `Desktop\Adventures_assets\`: the TileForge current release
  (road-layer retirement build, commit ae1eecb — 4 complete theme packages +
  reference pack with GAME-GUIDE/FORMATS/scenes/flagships) and a **new forge**:
  **Sprite Forge full pack** ("Bullet Hell Sprite Assembler", 2026-07-27).
- Sprite Forge contract (pixel-verified against the manifest — ranger.png is
  exactly 4 cols × 28 rows × 64 px): 231 actors — 12 player kits, 128 enemies,
  34 bosses (10 composite multi-part with motion metadata), 6 NPCs + 4 vendors,
  12 props, **20 projectiles + 15 effects incl. telegraph rings/cones/beams,
  charge-up, muzzle, impact**; manifest-driven 28-row humanoid rig; 64 px cells
  (32 logical × 2 scale); deterministic per-actor cfg regeneration; forest theme
  first, themes remap automatically. Designer: sprites are placeholders,
  polished versions swap in later (same ids/contract — contract frozen).
- **Approved batch ("seems fine") recorded:**
  1. Sprite Forge supersedes Actor Forge v2.3 as the actor/effects source
     (register CORE-20 row + new Tooling contracts entry).
  2. docs/12 §2.14 rewritten to the manifest-driven contract (importer + slice
     test read the manifest, never hard-code the rig); §2.5, §3.1, M2/M5/M6
     references updated; `spriteforge_importer` replaces `actorforge_importer`.
  3. M-FX rescoped to **curate + gap-fill** (three named gaps: player-vs-hostile
     family assignment, shared hostile signature as a rendering treatment,
     hazard arm-progress indicators); nine-row acceptance + end-of-M5 call
     unchanged. Mirrored in roadmap, GDD §15, questionnaire CORE-20.
  4. Asset location recorded: **drops committed to the game repo `assets/`**
     (`.gdignore`d raw drops; importers consume them) — game repo commit
     5dd2fd5, ~130 MB, no LFS needed (TECH-17 stance holds).
- Noted for M1: the packaged TileForgeImporter.cs is a C# reference
  implementation; pinned Godot is non-Mono, so M1 ports it to GDScript per
  FORMATS.md. Sprite pack is forest-theme; lab arena theme choice is an
  M1-time call (cosmetic mismatch acceptable in the lab).
  (Corrected during M1: the Godot importer ships as GDScript — the C# file
  is the Unity path; no port was needed.)
- Scope guard restated: 231 actors ≫ lab bill; the lab roster stays player
  shell + 6 enemies + 1 elite, hand-picked (ranger = Archer-shell candidate).

## Late night — M1 EXECUTED (TileForge proven; designer said "sure lets go")

- **Theme call:** dusk (the arena is a dungeon room; dungeon/dusk carries the
  menace per CORE-19). Catalog is identical across themes — swapping is a
  re-import, cheap to veto.
- **Package in-project:** dusk `tileforge/` copied to `res://tileforge/` per
  GAME-GUIDE §1; `tileforge.tres` (81 atlas sources) built headless via
  `addons/tileforge_importer/run_import.gd` — the shipped EditorScript hangs
  if instantiated headless, so the wrapper rebases it onto RefCounted first.
- **§4 acceptance green:** step 1 pixel-match — map.tmj rendered from stored
  gids, **0 differing pixels vs map-reference.png (2304×1536)**, first try,
  local AND CI Linux runner (no cross-platform compositing drift). Step 2
  scoped to the net16 path the arena uses: derived port masks for all 278
  road/pier/fence/wall/river cells match stored (incl. river→water and
  wall→gate special rules). Full blob47 derivation deferred to WorldForge
  integration (post-lab).
- **Arena:** 48×32 dungeon room per docs/12 §3.1 from `data/arena_lab.json` —
  perimeter, two corridor walls, corner pocket (2-cell mouth), open center
  16×12; underlay/wall/structure placements resolved once and consumed by
  both the runtime TileMapLayers and an offline preview compositor; boots
  headless (206 solid cells, 1742 placements).
- **DEVIATION — designer ruling pending:** §3.1's five 2×2 obstacle pillars
  became five **3-cell wall stubs**. Preview evidence (committed workflow,
  images in game repo `tests/pixel_match/`): slim pillar props = invisible
  walls; 2×2 wall blocks = hollow-ring art; monolith/rtower structures =
  art underfills the blocked footprint. Straight wall runs are the one
  asset whose art fills blocked cells edge-to-edge — Law 8 honest at cell
  granularity. Layout is [T]; intent (line-of-fire obstacles) preserved.
- **Bitgrid by construction:** `sim/collision/bitgrid.gd` baked from the same
  def as the visuals; collision-tint preview confirms art coverage of every
  solid cell.
- **CI:** M1 pixel-match + net16 job added (Linux runner, cached Godot
  4.6.2 download); gdformat now exempts the generated forge scripts — this
  also fixes the CI failure on the earlier asset-drop commit (the old check
  swept the shipped forge scripts). All jobs green on the M1 push.
- **Law 6 check:** preview reads quiet — floor is low-contrast dusk
  dungeon brick; designer eyeball pending alongside the stub ruling.
- Game repo commit d412ade. Hours note logged; session still open.

## Late night — M1 approved; M2 started; build sessions move to the game repo

- **Designer approved the M1 arena as shown** ("ye sure lets go for it" after
  reviewing both previews): Law-6 quiet floor confirmed; the wall-stub
  obstacle deviation from docs/12 §3.1 accepted (recorded in the §3.1 arena
  row and the arena def's comment).
- **M2 started:** `sim/pcg32.gd` (named-stream PCG32, serializable) and
  `sim/input_frame.gd` (quantized InputFrame per §2.4) landed, parse- and
  format-checked (game repo commit 73399bc). The full remaining M2 work list
  is in the game repo CLAUDE.md milestone tracker.
- **Workflow decision (designer):** build sessions now run rooted in the game
  repo (`C:\Users\headc\Documents\Wildshot-Adventures`) so its CLAUDE.md
  contract auto-loads — the M0 design intent. The planning repo remains the
  design authority; milestone records and design decisions still land here
  (game-repo sessions reach over by absolute path, exactly as this session
  did toward the game repo).
- Handoff opening prompt updated for the game-repo session.

## Morning (planning-repo session) — UI question + UI style kit pre-registered

- Designer asked what the plan needs for UI (menus, map, etc.). Answer from
  the docs: the lab's UI bill is tiny and fully scheduled (M3 options/remap,
  M4 HUD + death recap, M8 tester start screen); the full-game surface
  (map, collection book, inventory, tree, journal, vendors) is implied by
  locked decisions but deliberately undesigned until the Part II interface
  module — post-Gate-1, per the external review's lab-before-paper rule.
  Flagged early: the collection book is the endgame horizon (CORE-18) and
  deserves first-class design when that module opens.
- Designer floated building UI in a Claude design session; assistant's
  honest scope check: full-game screens no, a **UI style kit** yes — same
  forge model as the other asset packs. Designer committed ("i could make
  ui style kit i suppose").
- **`docs/13-UI_STYLE_KIT_SPEC.md` written** (Tier 1 = what M3/M4 consume;
  Tier 2 = M8 extras; big screens explicitly out of scope; machine-readable
  manifest; acceptance checklist incl. colorblind + Law 6 checks; frozen
  contract rule). Register Tooling contracts entry added, ranked strictly
  behind M-FX. M2's engineering was completed by the game-repo session in
  parallel (its records are the two prior commits).

## Early morning (game-repo session) — M2 EXECUTED; designer feel pass pending

All remaining docs/12 §4 M2 items built, tested, and committed (game repo
3ac9081 → db5ada1). Every engineering acceptance line is green; the two
human lines stay open below.

- **SimWorld spine (§2.1):** RefCounted core, no Nodes/physics/engine clock;
  `players` is an array (GDD-16 — no player singleton or `get_player()`
  anywhere); named PCG32 streams (`rng_enemy`/`rng_misc`; `rng_vfx`
  deliberately absent from sim); typed event queue; canonical `serialize()`
  + FNV-1a-64 `state_hash()`; external mutation only via a command queue
  drained at tick start. 1024-slot SoA projectile pool (M2 rig subset),
  free-list order serialized — allocation order is state.
- **Determinism smoke (§2.4):** double 1800-tick runs, scripted inputs:
  60/60 30-tick checkpoints byte-identical, all distinct; wall slide clamps
  at exactly 1.35 tiles (never penetrates, provably engages); 142 hits.
  New **windows-latest CI job** runs it per the §2.4 Windows-only scope.
- **RealtimeDriver (§2.1):** accumulator in `_process`, 5-tick catch-up cap
  then logged slew, `alpha()` for interpolation; pause stops stepping —
  full freeze with zero queued actions is structural (CORE-31).
- **Kinematics (§2.3):** circle body, axis-separated slide vs the bitgrid,
  closest-point corner test (grazing a corner the rounded body misses is
  not a collision). CORE-33 movement-only kit; firing writes no movement
  modifiers by construction (CORE-32).
- **HumanSampler + InputMap (§2.8):** quantized InputFrame per tick,
  tick-accurate edges (never per-render-frame `just_pressed`); code-
  registered remappable defaults incl. autofire_toggle; zero aim-assist
  code, stated in-file (CORE-50).
- **Camera (§2.9) + interpolation toggle:** fixed zoom, no smoothing,
  arena-clamped; positions rounded to whole base-res pixels view-side only;
  prev/curr lerp-vs-snap on F7. Interp-ON is a dev default only — the
  tester default remains the §6 item 1 high-refresh A/B's output.
- **Sprite Forge player (§2.14):** roster importer copies ONLY
  actor_sheet_map-listed actors (player → ranger) from the gdignored drop;
  ActorLibrary builds SpriteFrames fully manifest-driven; AnimatedActor
  derives facing view-side from sim velocity, renders 64 px cells at 0.5
  scale, freezes its animation clock during pause. **Actor-sheet slice test
  green** (ranger 256×1792 = 4 max frames × 28 rows × 64 px, all lab rows
  non-empty) and added to CI.
- **M2 STRESS VERDICT (§2.2/§2.3, due at M2, recorded):** rig held ~600
  live SoA projectiles + 24 stand-ins in the real arena, real renderer:
  min window **1950 fps** (vsync off), worst window avg frame **0.51 ms**
  vs the 16.67 ms budget, per-tick sim cost ~1 ms (~6% of a locked-60
  frame). A frame-gated spawner bug in the first run pushed ~900–1000 live
  at the same frame times — passes with wide margin either way. **Spatial
  hash stays deferred; C#/GDExtension escape hatch NOT triggered.**
  Evidence: `reports/stress_m2.json`, committed as a deliberate exception
  to the reports/* ignore rule (verdict evidence, not bot output).
- **Movement-speed editor (§3.2 M2 slice):** SET_MOVE_SPEED sim command,
  band clamp 3.0–5.5 enforced SIM-side, `replay_dirty` stamped and
  SERIALIZED (an edited run hashes differently — edits can't masquerade as
  clean evidence); F2/F3 presets 3.0/4.0, −/= steps, HUD readout with
  REPLAY-DIRTY marker. Smoke test asserts clamp, stamp, and hash
  divergence of an edited twin.
- Hours: dangling 03:20 start reconstructed (stop inserted at its 04:10
  last note); new session started 06:41 with notes through the M2 work.

**Addendum (same morning): "movement could be a little smoother" — triaged
mechanically.** Designer feel note logged PROVISIONAL (tired session; the
rested pass still owns the verdict). Built `tests/motion_probe/` —
instrumented drive phases measuring per-frame camera pixel steps + pacing
in interp AND snap modes (this doubles as tooling for the §6 item 1 A/B).
Findings on the dev display (60 Hz, vsync on): motion pipeline is CLEAN —
interp and snap both track ideal px/frame with zero stall frames; only
occasional OS-level 33 ms hitches. The probe's first run "caught" a stall
that turned out to be its own illegal spawn inside the west corridor wall
— kinematics correctly pinned the body against the face (ledger #8 filed:
axis clamp assumes legal spawns; scenario loader must validate at M4).
Likely felt cause: the 8 fps walk cycle (2 step-cycles/s at 4 t/s reads
as foot-sliding). Presentation lever applied, PROVISIONAL: walk rows now
12 fps at the 4.0 baseline, scaled live by actual velocity so 3.0/5.5
don't foot-slide (view-side only; sim untouched; determinism unaffected).
The rested feel pass evaluates this knowingly.

## Morning (game-repo session, continued) — M3 EXECUTED (engineering)

Designer said "lets go for m3"; all code-shaped M3 items built, tested,
committed, pushed (game repo 8a0f2d4→d172a25) in the same session. M2's
feel-pass line stays open and is unaffected.

- **Weapons as data (§3.3 exact):** longbolt/scattercast/wheelblade .tres
  (WeaponFrame + ShotDef resources; roster changes are data edits by
  design). Longbolt range cap honored (14 t/s × 28 ticks = 6.53 ≤ 6.5+ε).
- **Fire path (§2.4/§2.8, CORE-32):** tap/hold/autofire unify on one
  cadence gate; autofire is a sim-side latch (in replays; HUD reads sim);
  the weapons module sees no entity lists and calls no RNG — spawn is a
  pure function of (aim, player pos, weapon resource, volley index,
  tick). **Mechanized proofs now in the standing smoke test:** a fire run
  and a no-fire twin produce byte-equal per-tick positions (firing writes
  zero movement state) and identical RNG stream end-states (fire path
  draws nothing); cadence gate never breaches; the latch demonstrably
  fires during a fire_held gap.
- **Pool M3 form (§2.6):** damage/pattern/motion programs (straight,
  decelerate, sine, boomerang — pure functions of ticks-since-spawn) +
  the 8-slot hit registry with in-contact bits (one pass per crossing),
  max-passes rule, deterministic registry_full → DamageBlocked. One
  damage-resolution path; EntityKilled + death sweep. Typed despawn
  reasons. SERIAL_VERSION 2.
- **Terrain (§2.3):** circle-vs-bitgrid per tick (no tunneling at ≤14
  t/s), TERRAIN despawns + driver-side corner-snag JSONL
  (user://logs/terrain.jsonl, near-corner flagged).
- **Replay spine (§2.4):** .wsr = header {build id, scenario id, seed,
  weapon-data hash, speed config, replay-dirty, initial-state hash} +
  InputFrame stream + 30-tick checkpoints. Golden replay per weapon
  frame, **verified 10× consecutive, PASS** — the standing CORE-32
  regression test. Live capture always-on in the dev scene (F10 dumps).
  Dirty replays rejected as evidence at load. **Deviation flagged for
  designer veto:** the CI golden-replay gate (addendum row says M4) was
  activated at M3 on the Windows job — strictly more verification, zero
  design change, one-line revert if vetoed.
- **Remaps (CORE-50 "from start", honestly met):** Config autoload (first
  of the allowed four, zero gameplay state) owns settings.cfg
  (config_version 1, no dead keys); F1 remap panel, click-and-press,
  live + persisted in one path; persistence proven by a headless
  restart-cycle test against an injected path.
- **GIF pipeline (§2.10/§2.12):** F9 ring buffer (last 10 s @ 30 fps) →
  PNG sequence → tools/gif.ps1 (ffmpeg two-pass palette, 2× neighbor
  upscale).
- Scenario builders for replays live in tests/ until the M4 scenario
  picker makes scenarios data (noted in-file; not a ledger item — it is
  the plan's own sequencing).

**Key rebind (device constraint, same morning):** the dev keyboard has no
F-row, so every F-key default above is superseded — utility keys are now
O (options/remap panel), I (interp toggle), [ / ] (speed presets
3.0/4.0), G (gif dump), R (replay dump); Esc and -/= unchanged. The HUD
shows live key hints read from the actual InputMap. Standing rule
recorded in the game repo: future debug bindings avoid F1–F12.

**Feel-tuning round (same morning, designer-driven, PROVISIONAL like all
of today's feel data):**
- **§6 item 1 first data point:** designer on the 60 Hz dev panel:
  interpolated rendering "doesn't feel reactive enough to really dodge
  with" — interp's one-tick visual delay on direction changes. **Dev
  default flipped to SNAP** (toggle unchanged; the formal A/B on a
  high-refresh panel still owns the tester default, now with one real
  sample recorded).
- **Cadences tuned off v0** (data edits, the designed path): Longbolt
  24→18, Scattercast 30→24, Wheelblade 48→38 ticks ("faster attack
  speed"). §3.3 v0 stays the recorded starting hypothesis; DPS shapes
  shift accordingly; TTKBot re-derives sponge math at M5.
- Friendly projectile visuals: sphere placeholders (designer call,
  replacing a brief sprite experiment), motion-stretched along travel,
  rendered at 0.75× collision size ("a little smaller") — under-rendering
  friendly is player-favorable and Law-8-benign; hostile visuals stay
  full-size and round until the M-FX signature language.
- Tap buffer added sim-side (4 ticks): taps landing mid-cooldown fired
  nothing and read as unresponsive; now they fire at gate-open. Smoke
  test quantified: same scripted inputs, 40→45 volleys pre-tuning.
- Environment findings: 632 stutter spikes avg 30 ms with sim at 0.1 ms
  (spikes.jsonl instrumentation) → windowed compositing/GPU idle clocks;
  fullscreen now the boot default (Alt+Enter windowed); NVIDIA
  per-app "prefer maximum performance" recommended to the designer.

**Feel round 2 (same morning, all PROVISIONAL):** movement approved
after snap default + fullscreen + queue=1 retry ("feels good");
cadence step 2 (15/20/32); projectile punch pass — Longbolt 16 t/s /
ttl 24 (range 6.40, cap honored), Scattercast 12.5 / 19, ShotDef
spawn_offset (muzzle spawn, purity + data-hash preserved), muzzle +
impact flash pool (ledger #5 placeholder; CORE-32 feedback clause
honored — no shake/hit-stop). Audio's absence named as the remaining
"floaty" factor — arrives with the M6 cue map by schedule. Session
closed with designer: "feels good for now." Every feel call today
re-opens cheaply (data files + toggles) at the rested pass.

**Actor-source candidate surfaced (designer, same morning) — recorded, no
decision:** the designer's `8-bit sprite assembler` (separate repo,
actively developed; inspected READ-ONLY at their request). Facts: 24 px
4-direction rig with idle/walk/ATTACK/HURT (12-column contract), **259
enemy defs / 202 exported sheets** (more than the current pack's 128),
7 projectile + 6 impact + 6 status + 5 trail effects, and — the
differentiator — a full **equipment compositor** (weapons/shields/
outfits by tier, species, builds; 117 KB weapon renderer, outline
engine, weeks of readability regression work). Missing vs the current
frozen pack: cast/death/spawn/dash rows, a game-consumption manifest/
export contract, and stability — its own handoff documents an active
mid-repair state with unpushed checkpoints. Assistant recommendation
(designer to ratify): Phase A / Gate 1 stays on the frozen Sprite Forge
pack (richer rig the lab consumes today, zero mid-milestone churn); the
assembler is the natural **Phase B candidate** — visible equipment is
genre-core for an RotMG-like and baked sheets can never do it. Formal
decision point: the post-Gate-1 asset review, with prerequisites (cast+
death rows, frozen export contract). A styled A/B in the dusk arena is
on offer if wanted sooner.

## Late morning (game-repo session) — M4 EXECUTED (engineering)

All docs/12 §4 M4 items built, tested, committed, pushed. Highlights
beyond the tracker line: the §2.10 instrumentation list is now
demonstrable end-to-end — scenario picker + one-key reseeding reset
(seed logged), lowest-speed one-click preset, density meter with
per-faction split + budgets red-line, god flag that LOGS every absorbed
hit as DAMAGE_IMMUNE (replay-dirty), replay-valid slow-mo divisor,
event console tail, death recap naming pattern + telegraph lead
(tested via the debug emitter before enemies exist, per plan), hitbox
display drawn from sim shapes, damage numbers non-occluding with the
§2.5 band table now enforced in code (render_layers.gd), UI/text scale
visibly changing the HUD at runtime, and the split verdict command
writing verdicts.jsonl with PROVISIONAL auto-stamping (feel + bot =
rejected with the rule quoted). M4 acceptance lines all pass their
mechanical half; the standing human lines (rested feel) remain open
and gate nothing in M5 engineering. Sim serialization v7; goldens
regenerated deliberately at each format change and 10x-verified; CI
fully green.

**Actor-source decision APPROVED (designer, late morning): §2.14
Amendment v2 applied to docs/12; docs/14 is binding.** The 8-bit
assembler's game-pack export supersedes Sprite Forge as actor/effects
source. Designer is building the 1× exporter against docs/14 (their
assembler-side session is executing the spec). Game repo: importer v2 +
player swap on pack drop, BEFORE M5 enemies; Sprite Forge stays as
fallback until M5 acceptance passes on the new pack. Also fixed on
designer report ("didn't see an enemy"): the debug emitter had NO
visual — fired from an invisible point (Law-8 hole even in a dev
scene); it now renders a hostile diamond with a contracting windup
ring. Real enemies are M5, as scheduled.

**M3 items that are designer-owned and still open (accounts + public
posting are never the assistant's to do):**
1. Stand up the **itch page, devlog thread, and bare Discord** (M3 line;
   moved early from M7 by the plan to prime the tester pipeline).
2. **First weekly GIF post** (play → F9 → tools/gif.ps1 → devlog + one
   community). The weekly cadence starts here and never stops (§5).

**Open for designer (M2 acceptance, human lines):**
1. **Feel:** "movement feels responsive at 3.0 and 4.0 tiles/s" — run the
   game, F2/F3 to switch presets. Fresh-hands rule applies: this is a feel
   verdict, rested session, never the builder's enthusiasm at hour 14 —
   and today's session is long. Schedule it at a day start.
2. **Eyeball:** ranger renders + faces correctly from movement; pause
   fully freezes (Esc); F7 interp/snap difference visible.
Designer approval of these closes M2 in the tracker; M3 (aim/fire/
autofire + deterministic projectiles + replay capture + public channel)
is next per docs/12 §4.

---

## Session addendum — M5 session start (game repo cf54bcc)

**Full-enemy-catalog assembler pack integrated.** The designer shipped
`wildshot-full-enemy-catalog-pack-1x.zip` (57 families / 202 variants,
players byte-identical to pack v0, same tool commit + frame contract;
new manifest shape: `variant_system` + index files per the pack's
IMPORT_GUIDE). Game-repo import logic gained a shape adapter — enemies
resolve by `<family>:<variant>` key, roster misses fail loudly with the
family default suggested, and the imported manifest normalizes to the
flat `actors[]` shape roster-only, so every downstream consumer is
untouched. Catalog-shape fixture added to the consumer test. docs/14
note: both packs to date are `version: 0` previews — the §3 shape
freeze still binds at pack v1; the importer's dual-shape support is the
transition mechanism, not a contract change.

**Designer decisions (asked once, per the handoff):**
1. Catalog pack supersedes pack v0 immediately — approved.
2. M5 roster mapping: **Rusher = `wolf:gray`**, **Husk Archer =
   `skeleton:archer`** — recorded in `data/actor_sheet_map.tres`.

M5 engineering begins: EnemyDef family → 5-state machine → §3.4 v0
stats → assembler rendering + HP bars → scenario spawns → DodgeBot +
canaries → proofs. Prior session's dangling hours entry closed
honestly (stop backdated to its wrap note, zero minutes added).

---

## M5 ENGINEERING COMPLETE — 2026-07-27 (game repo cf54bcc → ffedcf6)

All docs/12 §4 M5 lines landed, each with mechanized evidence:

1. **EnemyDef family** (EnemyDef / EmitterSlot / PatternDef): CORE-40
   lean stats, §2.7 policy grammar (chaser/keep-range live; orbit/
   flanker declared, anchor until their M6 rows), ShotDef volleys shared
   with WeaponFrame. Explicit 5-state machine; timers in ticks; zero
   RNG drawn (lint + smoke prove it).
2. **Rusher + Husk Archer at §3.4 EXACT v0 stats**, rendering from the
   assembler catalog (wolf:gray / skeleton:archer per the designer's
   mapping). Smoke mechanizes the contracts: telegraph lead EXACTLY 12,
   fire period EXACTLY 90, contact cadence ≥ its data cooldown,
   keep-range settles 5.02, standing player dies EXPLAINABLY (recap
   names "Rusher contact" / "Husk Archer's aimed shot").
3. **Authored [T] values not in the §3.4 table** (flagging for the
   register, no design change intended): contact_cooldown 30t,
   post-fire recover 20t, husk trigger_range 7.0t (= shot reach),
   aggro_range 12t. All data-side, all .tres.
4. **DodgeBot per §2.11** + BootArgs (autoload 3/4). ONE implementation
   note: §2.11 says "16 directions + stay" but §2.4's InputFrame is
   {-1,0,1}/axis — the policy evaluates 16 HEADINGS and realizes the
   odd ones as alternating adjacent 8-dir frames, so every emitted
   frame is a legal human input (§2.8 indistinguishability preserved).
   Flagging as implementation-of-record; if the register wants literal
   analog input instead, that reopens §2.4, not this code.
5. **Canaries calibrate**: trivial MUST-PASS passes (near-miss 1.14t);
   undodgeable wall-ring MUST-FAIL fails every seed at t32 with .wsr
   repros. **Proofs committed** (reports/, mechanical-verification
   label): Rusher PASS (5×60 s, 3.0, ability-off, body clearance
   0.79t), Husk PASS (near-miss 1.14t), + first_contact composition
   bonus PASS (0.47t). NOTE: M5 enemies draw no RNG, so seeds are
   identical runs today — seed variation becomes real when rng_enemy
   authored variation lands (M6).
6. **Debug emitter retired** (its precondition — enemies spawnable from
   the scenario picker — landed same session). SERIAL v9; goldens
   regenerated deliberately (twice this session: v8 enemies, v9
   retirement).
7. **Acceptance instrumentation**: §2.5 band assertions at every boot;
   recap telegraph-lead keyed by pattern; TTK evidence lines in
   session.jsonl; §2.6 sustained worst-case LIVE in the density meter.
8. **Stress-density screenshot audit** committed
   (reports/density_audit_m5.png): hostile fire legible above max
   player VFX + full damage numbers (Laws 1/2). The meter red-lined the
   audit's deliberate 26/24 enemy over-budget — over-detection works.
   Law-3 hostile SHAPE signature remains M-FX/M6 scope as planned.
9. **Spriteforge fallback retired** per Amendment v2's own terms; CI
   slice step removed WITH it (CI change, flagged here for veto — the
   assembler pack test owns sheet validation now).

**M-FX pre-registered decision is now DUE (end of M5, ~week 7):**
curated effects pack vs law-compliant placeholders for the tester
build. Designer call; the 9-row acceptance applies to whichever ships.

Open designer-owned: rested day-start pass (closes M2, ratifies all of
today's provisional feel — rusher chase read, 12-tick telegraph read,
HP bars); weekly GIF #2 (wolves + skeleton archers are fresh material).

---

## Arena detail pass + the proof system's first real catch (game repo → 02ed154)

Designer (playing the M5 build): "the game looks great rn" (PROVISIONAL,
marathon day) + requested a more detailed map, styled on the tile
reference pack's scenes. Ruling applied: enriching THE one lab arena
stays inside SPEC-A's one-arena bill (§2.11 already names its corner
pocket); the §3.1 [T] skeleton (perimeter, two corridors, pocket, five
stubs, open center) is preserved untouched; detail layers on top.

**Builder v2 (contained):** arena defs gain floor_patches / decals
(cosmetic only) / props (per-entry solid flag, reserved for chunky
cell-filling art per the M1 honesty ruling). The reference pack's
dungeon-room dressing recreated: crypt aisles, shrine room with
templefloor dais + altar + braziers + chest, pocket bait, prop
clusters, bones/blood/webs/cracks decals. A loud stone floor patch was
cut on sight (Law 6). NOTE: the designer's "use one of the reference
pics" was honored as style guidance rendered through OUR builder — the
showcase-rich .tileforge.json project file was explicitly NOT consumed
(that would mean reimplementing the TileForge compiler; the pack
boundary doc forbids it).

**The catch:** the detail pass BROKE the Rusher dodge proof — the bot
fled radially, got pocketed against the west wall (heatmap: 573 ticks
pinned SW), died to contact cadence. Exactly the falsifiability the
proof system exists for: a map change invalidated a shipped pattern's
proof and blocked the seam until fixed. Three iterations, recorded:
(1) aisles pulled 2 cells east + horizon 45 + kite tiebreak — still
cornered (identical trajectory: the clearance key rewarded radial
flight); (2) mode split (positioning decides when fully safe) — still
cornered (linear body extrapolation lies in corners); (3) the real
fix: the policy now SIMULATES chaser pursuit coupled to each candidate
path (projectiles stay closed-form per §2.11), horizon 60, positioning
= threat distance minus wall proximity. Orbit emerges; no scripted
orbit policy pulled forward from M7.

**Correction to the game-repo commit message:** it says the failing
repro was committed — it was not (reports/*.wsr stay gitignored by the
§2.11 plumbing rule; the .wsr dumps were local adjudication artifacts
of the interim states). The fix history lives in this log instead.

**Final proof state on the detailed arena:** Rusher PASS near-miss
1.18 (up from 0.79 on the plain arena — the pursuit-aware bot kites
with more margin), Husk PASS 1.68, first_contact composition PASS at
0.121 — TIGHT, flagged: composition margins shrank with geometry
detail; the M7 composition tier should watch this number per scenario.
Canaries calibrate through both the map and policy changes. Goldens
verify untouched. 238 solids (was 206).

---

## Forest Walk testbed (game repo 0ad8f38) — designer-approved SPEC-A addendum

Designer clarified intent behind the map request: see CHARACTERS AND
PROJECTILES in a natural open setting — the sprite-reference pack's
"judge every sprite composited over these" doctrine, in motion. RULING
RECORDED: a second, natural-setting arena joins the lab as a designer-
approved addendum to the SPEC-A one-arena bill. It is a readability
testbed: zero rewards, zero new mechanics, reachable via the scenario
picker, and it carries a passing composition proof like anything
tester-reachable.

Engineering: builder v3 stays data-driven — blob-47 masked coverages
(grass feathering onto soil via the tool's own autotile set), tree
props with crowns in a new CANOPY render band (occludes bodies only;
band asserts extended: bars/threats always above foliage), solid
tree-line borders (every band cell trunked — collision == visuals).
Scenarios now name their arena; the lab defs and all M5 evidence are
untouched (smoke hashes byte-identical, goldens verify untouched).

Proof: forest_walk composition PASS (3 seeds x 60 s, 3.0, ability-off,
near-miss 0.125). Both composition margins (first_contact 0.121,
forest_walk 0.125) now sit an order of magnitude below the solo-proof
margins — the M7 composition tier should treat ~0.1 as the number to
watch.

Full-fidelity reference maps: NOT built by reimplementing the TileForge
compiler (boundary honored). The tool already exports compiled map.tmj
— flagged to the designer: export a scene as tmj + a solids convention
and the game grows a direct importer.

---

## DESIGNER DECISION: every enemy attack is visible (projectile / slash)

Designer, playing Forest Walk: "i think all enemies should have
projectiles or some sort of slash etc". Recorded as a design principle
amending the §3.4 roster's Rusher row [T]:

- **Principle:** every enemy attack manifests as a telegraphed,
  visible projectile or arc — no silent contact-only damage. (Law 8
  strengthened: "Rusher's slash, telegraphed 0.17 s before" beats
  "contact". Also the more RotMG-authentic reading — bodies threaten
  via attacks, not touch.)
- **Rusher v0 row becomes:** hp 20, r 0.30, spd 2.7 (kiteable at 3.0,
  unchanged), attack = 3-shot 50° slash arc, dmg 8, shot 5 t/s r 0.22,
  reach ≈ 1.1 tiles (ttl 10), telegraph 10 ticks, cooldown 36 ticks,
  trigger range 1.2. Contact damage 0. Law 4 ordering intact
  (slash dmg 8 / telegraph 10 < Husk 10 dmg / 12 ticks).
- Contact-damage MECHANISM stays in the sim (a future def may want
  it); the roster just stops using it. M6 roster rows all carry
  patterns already — the principle binds them as authored.
- Implementation is pure data (the 5-state machine + EmitterSlots
  already do this); DodgeBot gains a soft keep-away positioning term
  for ALL live enemy bodies (hard collision threats remain
  contact-damage bodies only). All proofs re-run.

**Slash decision executed (game repo 9871a96).** Pure data as expected;
the wolf now faces its aim and plays its attack row on every slash, and
recaps name "Rusher's slash" with its telegraph lead. Point-blank
detail: the 3-shot arc can stack (up to 24) on a target standing inside
the wolf — deep contact priced accordingly; grazing the arc edge costs
8. Bot re-learned melee in three recorded steps (birth-ring windup
hazards; explicit orbit stationkeeping around the local threat
centroid; melee trigger ranges as do-not-enter bubbles under simulated
pursuit).

**ADJUDICATION FLAG (§2.11 path):** with the slash roster, at 3.0
ability-off — Rusher solo PASS, Husk solo PASS, canaries calibrate,
forest_walk composition PASS, but **first_contact composition FAILS**
(13 hits, first@1050; repro dumped locally). Three slashing wolves +
two archers on the detailed dungeon geometry exceeds the PRIMARY
policy. Note the asymmetry: the slash redesign is EASIER for humans
(telegraphed, reactable) but harder for the conservative bot (bigger
no-enter bubbles); reactive windup-dodging is exactly the M7 secondary-
policy tier. Options for the designer: (a) retune first_contact's
spawn set (e.g. 2 wolves, or wider spread), (b) accept the known-hot
default until M7 policies re-adjudicate, (c) rule the composition
honestly-too-hot at lowest speed. Committed as FAILING — never
laundered.

---

## Projectile pack integrated (game repo 7b0fca8) — M-FX DECISION: CURATED PACK

The designer shipped wildshot-projectiles-all v0 (five interchangeable
styles; per-sprite contract: canvas/frames/oriented/§2.5 band/
covers_hitbox; native 1x; suggested_pattern_ids pre-seeded the Phase-A
mapping). Integrated within the hour: map-filtered importer with a
Law-2/8 guard (mapped hostile shots MUST declare covers_hitbox — CI
enforces), per-pattern sprite rendering (pack palette untinted — the
pack owns each style's hostile signature), oriented rotation,
Wheelblade two-frame spin at the pack's ~150 ms, hazard zone sprites +
the 8-step arm-progress strip (cool-modulated friendly per pack notes),
sphere fallback for anything unmapped, style v0 as default — restyling
is a one-file remap.

**The pre-registered M-FX decision (due end of M5) is SATISFIED by
delivery: curated pack, not placeholders.** The M6 nine-row acceptance
applies to this pack.

Provisional mapping notes for the designer: pattern 11 (Rusher slash,
decided after the pack's role table) borrows the fang — flagged that a
pack rev may want a dedicated slash shape so Fanmaw keeps sole claim on
fangs (Law 3 per-family shape language). Nova-ring + cast-flash wiring
into the flash view remains open (nova currently uses the generic
flash), tracked for the M6 EffectLibrary pass alongside the density/
opacity scaler + flash reduction.

M6 roster (Leadshot/Fanmaw/Ringer/Blightcaster) UNPAUSED: the pack
pre-assigned their shot sprites (dart/fang/burr + hazard zone), so the
next session authors patterns against real art from the first minute.

---

## 2026-07-28 (post-midnight): WorldForge consumer-prep landed (game repo 38150c7)

Designer returned from work, reviewed the doc-15 branch (fast-forwarded
onto the working branch), and re-ruled step 3's timing: the CONSUMER-
PREP half is pulled forward (recorded in doc 15 with rationale —
M0–M5 ran ~5 weeks ahead of the docs/12 schedule; the fixture-first
playbook integrated two packs in under an hour each yesterday). The
consumption half (TileMapLayers render, scenarios, POI/spawn use)
stays post-Gate-1 behind its own generated-test-arena ruling.

Landed: addons/worldforge_importer — packFormat-1 validator mirroring
the exporter's refusal rules (manifest sanity, per-file sha256 parity,
walkability decode into the sim Bitgrid with solid = NOT walkable,
spawnCell walkability, flood-fill == floodCount as the contract's own
§3.3 verification affordance, TMJ structural checks, artifactFormat
cross-check). Fixture: hand-derived flood 124 incl. a sealed pocket
(flood != total-walkable proven); three negative variants prove the
guards fail correctly. CI gate added.

Cross-checks noted while reading the WorldForge plan: the Godot verify
half is ALREADY closed tool-side (flood 33890 == TS 33890; 170 green)
and settlement pins landed as behaviors 37/38 — doc 15's step-2
"pending" note is stale on that point. The designer's only upstream
blocker for slice drafting remains the dusk package export (their
TileForge task). Flood-connectivity assumption recorded: validator uses
4-neighbor; a real pack disagreeing fails loudly at the flood gate.

OPEN blob47 flag from earlier still awaiting the designer's register
note: game-side blob47 exists only for hand-authored arena coverages
(Forest Walk grass); suggested reading = temporary, retires if/when a
WorldForge-exported arena replaces the hand-authored testbed.

**Phase-4 handoff, first contact with a REAL pack (game repo → 38150c7+):**
the WorldForge agent relayed the reference pack
(small-cold-coastal, packFormat 1, behaviorVersion 46). The game-side
validator PASSED it first try in 0.57 s: 8/8 hashes, walkability →
bitgrid, flood 33845 from (241,124) reproduced independently — three
implementations now agree. Contract-as-built notes to relay BACK to
WorldForge (per the don't-patch-packs protocol; no pack bug, just doc
truth): (1) world.json's own format field is `formatVersion` (the plan
doc's §3.2 example reads as `artifactFormat` inside world.json);
(2) validation-report shape confirmed {status, errors, warnings} with
warnings legal. Validator additions: report-status gate,
baseArtifactSha256 == files['world.json'] self-consistency. Dusk pack
requested by the designer from the WorldForge side; it will validate
identically (theme-blind checks, manifest-keyed). Consumption half
still waits post-Gate-1 per the re-ruling.

**Dusk pack delivered + validated (2026-07-28 00:47):** the WorldForge
agent shipped small-cold-coastal-pack-dusk (tileforge
dusk-ae1eecb-seed103991, new baseArtifact 702c10e6…); the game-side
validator passed it in 0.56 s with flood 33845 from (241,124)
reproduced — walkability theme-independence empirically confirmed
(same collision truth, different clothes). WorldForge recorded the
game's contract-as-built notes in its plan §3.3a (formatVersion
mirroring, files map, baseArtifact self-consistency, report shape).
The dusk-first consumption prerequisite (§6.1) is now satisfied
pack-side; everything world-gen waits only on the designer's
generated-test-arena ruling.

---

## RULING (designer, 2026-07-28 ~00:50): GENERATED TEST ARENA approved

"so will we get to test a generated map now?" — the doc-15 decision-3
escape hatch is exercised: the CONSUMPTION half lands now for ONE
world as a readability/traversal testbed (same class as Forest Walk:
zero rewards, no mechanics, picker scenario + composition proof).
Scope: dusk pack only (small-cold-coastal-pack-dusk), TileMapLayers
from the pack's resolved TMJ, collision + player spawn from
walkability.json, a small enemy set near spawn. POIs/settlements/
minimap consumption stays future work. Pre-flight check PASSED: the
game's imported TileForge package (sourceCommit ae1eecb, seed 103991)
IS the pack's pinned package (dusk-ae1eecb-seed103991) — resolved
layers will render against identical atlases.
